import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vue_capacitor_webworker',
  appName: 'vue-capacitor-webworker',
  webDir: 'dist'
};

export default config;
